spring:
  redis:
    redisson:
      config:
        single_server_config:
          address: redis://192.168.1.10:6379
          database: 11
          password: be_api
  datasource:
    druid:
      url: jdbc:mysql://192.168.1.3/activity_engine?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
      username: root
      password:
proxy:
  enable: true